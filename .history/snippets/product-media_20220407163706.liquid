{% case media.media_type %}
    {% when "image" %}
        <img src="{{ media | img_url: 'large' }}" alt="{{ media.preview_image.alt | escape }}" class="w-full h-full object-cover">
    {% when condition %}
    
        
    {% else %}
        
{% endcase %}
