<div class="h-full flex items-center justify-center py-12 px-6 md:px-4 lg:px-8">
    <div class="max-w-md w-full space-y-8">
        <div>
            <h1 class="text-center text-3xl font-bold mt-6"> Sign in to your account </h1>
            <div class="mt-8">
                {% form 'customer_login' %}
                    {% if form.errors %}
                    {%- comment -%}role="alert": この要素が新しく生成されたことをスクリーンリーダに伝える。SEO?{%- endcomment -%}
                        <div class="relative border border-red-700 bg-red-400 px-4 py-3 " role="alert">
                            <strong class="font-bold text-gray-900">Error!!</strong>
                            <p class="block sm:inline text-gray-500">{{ form.errors | default_errors }}</p>
                        </div>
                    {% endif %}
                    <div class="my-2">
                        {%- comment -%}sr-only: スクリーンリーダーのみで表示される。画面上は消える{%- endcomment -%}
                        <label for="email_address" class="sr-only">Email address</label>
                        <input
                            type="email"
                            name="customer[email]"
                            id="email_address"
                            autocomplete="email"
                            class="relative block w-full px-3 py-2 border focus:outline-none"
                            placeholder="Email address"
                        >
                    </div>
                    {% if form.password_needed %}
                        <div class="my-2">
                            {%- comment -%}sr-only: スクリーンリーダーのみで表示される。画面上は消える{%- endcomment -%}
                            <label for="password" class="sr-only">Password</label>
                            <input
                                type="password"
                                name="customer[password]"
                                id="password"
                                autocomplete="current-password"
                                class="relative block w-full px-3 py-2 border focus:outline-none"
                                placeholder="Password"
                            >
                        </div>
                    {% endif %}
                    <div>
                        <button type="submit" class="relative w-full py-2 px-4 text-sm font-medium text-white bg-gray-600 hover:gray-900">
                            Sign in
                        </button>
                    </div>
                {% endform %}
            </div>
        </div>
    </div>
    <div class="max-w-md w-full space-y-8">
    </div>
</div>